# Kairos — Open Items

Everything else has been stripped out (last cleanup: Jul 23) — this file now tracks only the financial aid buildout and the mobile App Store push. See git history for the full log of prior completed/dropped work if you need it back.

---

### Mobile app / App Store submission (plan approved Jul 23)

Submission path decided after research (Jul 23): **EAS Build + EAS Submit** (Expo cloud builds, auto-managed certificates, uploads straight to App Store Connect), then TestFlight internal testing, then review. Auto-crawl screenshot platforms (fastlane snapshot etc.) were evaluated and rejected: overkill for a ~5-screen single-locale app, and raw captures are the wrong deliverable anyway since Apple OCR-indexes caption text as a ranking signal. Screenshots will be captured in the iOS Simulator with a seeded demo account and composed into framed, captioned 1320x2868 marketing images with a local script using the Kairos design system.

**Phase 1: the app itself (built Jul 23)**

- [x] `mobile/app.json` store config: display name "Kairos Admissions" (matches domain strategy, avoids the "Kairos Educational Services" name conflict), bundle ID `com.kairosadmissions.app`, deep-link scheme, `supportsTablet: false` (skips the otherwise-required iPad screenshot set), real icon/splash (serif K, forest green on parchment, generated via Playwright script).
- [x] Auth expansion: email/password (shipped in scaffold) plus Google (Supabase OAuth via in-app browser + deep-link redirect, reuses the web app's Google provider, `mobile/src/lib/oauth.ts`) plus **Sign in with Apple** (native, via `signInWithIdToken`, availability-gated). Apple sign-in is mandatory under guideline 4.8 because the app offers Google login; it also fixes the real gap that web students who signed up with Google have no password to type into the app. Supabase client switched to PKCE flow. NOTE: Google/Apple can't be exercised until the Phase 2 dashboard config below is done; only code-complete for now.
- [x] Screens beyond the scaffold: React Navigation tabs (Matches/Timeline/Essays/Profile) + native-stack details. School detail (percentage headline + factors breakdown), Task detail with complete toggle (same client-side `timeline_items` update + RLS the web app uses, optimistic with revert), Essay feedback history (reads `essay_feedback_history`), Profile/Settings.
- [x] **In-app account deletion** (guideline 5.1.1(v), hard requirement): `delete-account` Supabase edge function (deployed, verify_jwt on; source in `supabase/functions/delete-account/index.ts`) mirroring the table-cleanup list in `src/app/api/account/delete/route.ts` (the web route is cookie+origin gated so the app can't call it directly). Keep the two cleanup lists in sync when adding tables. Verified end to end Jul 23 with a throwaway user: 200, auth user gone.
- [x] Verification pass of every screen with a seeded student (student1@test.com, recreated Jul 23 after test-student cleanup; `scripts/seed-demo-realistic.mjs` needed an `intended_major` array fix for the current schema and still dies at the retired `war_room_comments` table, after seeding everything the app needs). Screens verified via the Expo web target and captured to `screenshots/mobile-0*.png` — **this Mac has no Xcode, so the iOS Simulator itself couldn't run**; native-rendering check happens on TestFlight (or install Xcode and rerun). `scripts/seed-demo.mjs` is stale vs the current schema (pre-GPA-split) and needs a rewrite before reuse.

**Phase 2: store prep, in execution order (detailed breakdown Jul 23; free-tier question settled: free Apple accounts can't distribute at all, not even TestFlight, and fee waivers only exist for nonprofit/edu/gov orgs, so the $99/yr is unavoidable)**

1. [ ] **Ari: Apple Developer enrollment ($99/yr)** at developer.apple.com/programs/enroll — the long pole (~48h approval), gates steps 5+. Decision: enroll as individual (fast, needs 18+, seller shows as personal name, convertible to org later) vs LLC (needs a D-U-N-S number first, ~a week). Recommended: individual now given launch timeline.
2. [ ] **Ari: free Expo account** (expo.dev/signup) then `npx eas login` from `mobile/`. Can happen today, independent of step 1.
3. [ ] Claude: `eas init` + `eas build:configure` (links project, writes build config). Needs step 2.
4. [ ] **Ari (Claude dictates exact values): Supabase dashboard auth config** — enable Apple provider with client ID `com.kairosadmissions.app`, add `kairosadmissions://` redirect to the auth allowlist. Without this, Google/Apple sign-in fail in production builds (email/password unaffected). Can happen anytime.
5. [ ] **Ari (one-time): first `eas build --platform ios --profile production`** — the first build creates signing credentials and requires an interactive Apple ID login, so Ari runs it; EAS stores credentials after that and Claude runs all future builds.
6. [ ] TestFlight: Claude runs `eas submit --platform ios`; Ari installs via TestFlight on his phone and exercises every screen. **This is the real native-rendering check** (no Xcode on the dev Mac; Phase 1 was verified via the Expo web target only). Fix anything that feels off before review.
7. [ ] Listing: Ari creates the app record in App Store Connect (name "Kairos Admissions", bundle ID, SKU `kairos-ios-1`, price Free). Metadata draft (name/subtitle/keywords/description/promo text, privacy nutrition label + age rating answers, review notes) done Jul 24 in `docs/app_store_listing_draft.md` — needs a human read before pasting in, and still needs a real reviewer demo account created + live support/privacy/marketing URLs. Screenshot-framing script (`scripts/frame-screenshots.mjs`, sharp-based, parchment/forest-green Kairos styling) done Jul 24 and run once against the current `screenshots/mobile-0*.png` captures → `screenshots/framed/`; rerun once final reviewer-account captures exist. Ari pastes/uploads and submits for review.
8. [ ] Review (1-3 days). Pre-cleared rejection risks: account deletion (shipped + tested), Sign in with Apple (shipped), 4.2 minimum functionality (real native app), demo account (will be fresh). If rejected, paste the Resolution Center message to Claude for a fix + resubmit.

Realistic total: ~a week end to end, dominated by the two Apple waits (enrollment, review).

iOS only for now; Google Play deferred (separate $25 fee, 12-tester requirement for new personal accounts, same codebase later).

---

### Financial aid buildout

Merit aid likelihood and FAFSA/CSS Profile timeline tagging are shipped. The consent/UX pass this needs is also shipped: `FinancialAidConsentSection.tsx` (mounted on Profile) collects income bracket (ranges, not exact figures), state, and family size behind an explicit opt-in checkbox with plain-language "never required" framing; persisted via nullable `financial_aid_info_consent/income_bracket/state/family_size/updated_at` columns (migration `069_financial_aid_consent.sql`, applied). Unchecking consent clears the stored values.

- [x] **Net price calculator equivalent** across matched schools — `NET_PRICE_ESTIMATE_PROMPT` (`lib/anthropic.ts`) + `lib/net-price-estimate.ts` produce a rough estimated net-price RANGE per school from income bracket/state/family size plus real College Scorecard sticker-price/avg-net-price data where available (`lib/college-scorecard.ts`). Cached in `financial_aid_net_price_cache` (migration `077_net_price_estimate_cache.sql`, applied), keyed by school + income bracket + family size + state — never by student. Served via `POST /api/net-price-estimate`, hard-gated on `financial_aid_info_consent` + a real (non-"prefer_not_to_say") income bracket; students without that data see an inviting "add your financial info" prompt (`NetPriceEstimateSection.tsx`) on the School Detail page's Info tab, never a broken/blank calculator. Always shown with a "rough estimate, not a guarantee" disclaimer and a link out to the school's own official Net Price Calculator as the authoritative source.
- [x] **Scholarship ↔ aid-gap coalescence** — same `/api/net-price-estimate` response joins the estimate against `getFitTier`/`getMatchFactors` (`lib/scholarships.ts`), surfacing up to 5 Strong-Fit/Possible tracked scholarships as "could help close this gap" right under the estimate on School Detail — no duplicate second surface on the Scholarships page.
- [x] **Affordability as a real matches factor** — `schoolMatchingPrompt`'s AFFORDABILITY SIGNAL section reasons about exact household income/state/family size (when consent + real data are present) as a genuine factor in `why_text` (e.g. flagging a likely financial stretch at a high-sticker reach/target, or a likely-affordable in-state public safety) — never invents a dollar figure, never activates for a student without real opted-in data, and doesn't change behavior at all for students who left it blank. `matches/generate/route.ts` builds this signal server-side and only includes it in the prompt when genuinely present. `PROMPT_VERSION` bumped to `2026-07-24`.
- [x] **Loan/debt calculator (simple estimator, decided 2026-07-24)** — given the estimated net-price gap and what a family expects to cover per year, `lib/loan-estimate.ts` computes a rough total loan need over 4 years and a monthly repayment figure (illustrative fixed rate, 10-year term, clearly labeled as an assumption). Pure arithmetic, no AI call, no new storage. Surfaced inline on School Detail's estimate card. Always paired with a "not financial advice" disclaimer — no recommendation to borrow, no loan offer.

**Scope reversal (2026-07-24):** exact-dollar income storage and the loan calculator were originally out of scope; both were reversed on request. Income is now collected as an exact annual figure (`profiles.financial_aid_income_exact`, migration `078_financial_aid_exact_income.sql`, replacing the old bracket column) rather than a range, still fully gated behind the existing consent opt-in. The net-price cache (`financial_aid_net_price_cache`) now keys on income rounded to the nearest $10k band (`income_band`, migration `079`) rather than the raw exact figure, so it stays meaningfully shared across families instead of fragmenting to near-1-row-per-student.

First-gen / low-income framing care still applies: never gate core product value behind financial-data entry, keep it clearly optional, and pair any estimate/loan figure with plain-language context rather than assuming financial literacy.

Still not built, and not reversed: a real per-school Net Price Calculator API integration. No unified API for that exists across schools — the estimate remains AI-reasoned from public sticker-price/avg-net-price data plus the income/state/family-size inputs, always presented as an estimate, not an official figure.
